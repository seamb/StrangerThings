import React from 'react';

const Home = ({user}) => {
    return(
        <h1>
            WELCOME HOME! 
            {/* {user.username} */}
            {/* .charAt(0).toUpperCase() + user.username.slice(1)  */}
        </h1>
    )
    }

export default Home;